// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/help"
	"backend/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HelpQuery is the builder for querying Help entities.
type HelpQuery struct {
	config
	ctx        *QueryContext
	order      []help.OrderOption
	inters     []Interceptor
	predicates []predicate.Help
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HelpQuery builder.
func (hq *HelpQuery) Where(ps ...predicate.Help) *HelpQuery {
	hq.predicates = append(hq.predicates, ps...)
	return hq
}

// Limit the number of records to be returned by this query.
func (hq *HelpQuery) Limit(limit int) *HelpQuery {
	hq.ctx.Limit = &limit
	return hq
}

// Offset to start from.
func (hq *HelpQuery) Offset(offset int) *HelpQuery {
	hq.ctx.Offset = &offset
	return hq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hq *HelpQuery) Unique(unique bool) *HelpQuery {
	hq.ctx.Unique = &unique
	return hq
}

// Order specifies how the records should be ordered.
func (hq *HelpQuery) Order(o ...help.OrderOption) *HelpQuery {
	hq.order = append(hq.order, o...)
	return hq
}

// First returns the first Help entity from the query.
// Returns a *NotFoundError when no Help was found.
func (hq *HelpQuery) First(ctx context.Context) (*Help, error) {
	nodes, err := hq.Limit(1).All(setContextOp(ctx, hq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{help.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hq *HelpQuery) FirstX(ctx context.Context) *Help {
	node, err := hq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Help ID from the query.
// Returns a *NotFoundError when no Help ID was found.
func (hq *HelpQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(1).IDs(setContextOp(ctx, hq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{help.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hq *HelpQuery) FirstIDX(ctx context.Context) int64 {
	id, err := hq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Help entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Help entity is found.
// Returns a *NotFoundError when no Help entities are found.
func (hq *HelpQuery) Only(ctx context.Context) (*Help, error) {
	nodes, err := hq.Limit(2).All(setContextOp(ctx, hq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{help.Label}
	default:
		return nil, &NotSingularError{help.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hq *HelpQuery) OnlyX(ctx context.Context) *Help {
	node, err := hq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Help ID in the query.
// Returns a *NotSingularError when more than one Help ID is found.
// Returns a *NotFoundError when no entities are found.
func (hq *HelpQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(2).IDs(setContextOp(ctx, hq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{help.Label}
	default:
		err = &NotSingularError{help.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hq *HelpQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := hq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Helps.
func (hq *HelpQuery) All(ctx context.Context) ([]*Help, error) {
	ctx = setContextOp(ctx, hq.ctx, "All")
	if err := hq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Help, *HelpQuery]()
	return withInterceptors[[]*Help](ctx, hq, qr, hq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hq *HelpQuery) AllX(ctx context.Context) []*Help {
	nodes, err := hq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Help IDs.
func (hq *HelpQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if hq.ctx.Unique == nil && hq.path != nil {
		hq.Unique(true)
	}
	ctx = setContextOp(ctx, hq.ctx, "IDs")
	if err = hq.Select(help.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hq *HelpQuery) IDsX(ctx context.Context) []int64 {
	ids, err := hq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hq *HelpQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hq.ctx, "Count")
	if err := hq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hq, querierCount[*HelpQuery](), hq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hq *HelpQuery) CountX(ctx context.Context) int {
	count, err := hq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hq *HelpQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hq.ctx, "Exist")
	switch _, err := hq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hq *HelpQuery) ExistX(ctx context.Context) bool {
	exist, err := hq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HelpQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hq *HelpQuery) Clone() *HelpQuery {
	if hq == nil {
		return nil
	}
	return &HelpQuery{
		config:     hq.config,
		ctx:        hq.ctx.Clone(),
		order:      append([]help.OrderOption{}, hq.order...),
		inters:     append([]Interceptor{}, hq.inters...),
		predicates: append([]predicate.Help{}, hq.predicates...),
		// clone intermediate query.
		sql:  hq.sql.Clone(),
		path: hq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Help.Query().
//		GroupBy(help.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hq *HelpQuery) GroupBy(field string, fields ...string) *HelpGroupBy {
	hq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HelpGroupBy{build: hq}
	grbuild.flds = &hq.ctx.Fields
	grbuild.label = help.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Help.Query().
//		Select(help.FieldCreatedAt).
//		Scan(ctx, &v)
func (hq *HelpQuery) Select(fields ...string) *HelpSelect {
	hq.ctx.Fields = append(hq.ctx.Fields, fields...)
	sbuild := &HelpSelect{HelpQuery: hq}
	sbuild.label = help.Label
	sbuild.flds, sbuild.scan = &hq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HelpSelect configured with the given aggregations.
func (hq *HelpQuery) Aggregate(fns ...AggregateFunc) *HelpSelect {
	return hq.Select().Aggregate(fns...)
}

func (hq *HelpQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hq); err != nil {
				return err
			}
		}
	}
	for _, f := range hq.ctx.Fields {
		if !help.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hq.path != nil {
		prev, err := hq.path(ctx)
		if err != nil {
			return err
		}
		hq.sql = prev
	}
	return nil
}

func (hq *HelpQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Help, error) {
	var (
		nodes = []*Help{}
		_spec = hq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Help).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Help{config: hq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(hq.modifiers) > 0 {
		_spec.Modifiers = hq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hq *HelpQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hq.querySpec()
	if len(hq.modifiers) > 0 {
		_spec.Modifiers = hq.modifiers
	}
	_spec.Node.Columns = hq.ctx.Fields
	if len(hq.ctx.Fields) > 0 {
		_spec.Unique = hq.ctx.Unique != nil && *hq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hq.driver, _spec)
}

func (hq *HelpQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(help.Table, help.Columns, sqlgraph.NewFieldSpec(help.FieldID, field.TypeInt64))
	_spec.From = hq.sql
	if unique := hq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hq.path != nil {
		_spec.Unique = true
	}
	if fields := hq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, help.FieldID)
		for i := range fields {
			if fields[i] != help.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hq *HelpQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hq.driver.Dialect())
	t1 := builder.Table(help.Table)
	columns := hq.ctx.Fields
	if len(columns) == 0 {
		columns = help.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hq.sql != nil {
		selector = hq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hq.ctx.Unique != nil && *hq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range hq.modifiers {
		m(selector)
	}
	for _, p := range hq.predicates {
		p(selector)
	}
	for _, p := range hq.order {
		p(selector)
	}
	if offset := hq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (hq *HelpQuery) ForUpdate(opts ...sql.LockOption) *HelpQuery {
	if hq.driver.Dialect() == dialect.Postgres {
		hq.Unique(false)
	}
	hq.modifiers = append(hq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return hq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (hq *HelpQuery) ForShare(opts ...sql.LockOption) *HelpQuery {
	if hq.driver.Dialect() == dialect.Postgres {
		hq.Unique(false)
	}
	hq.modifiers = append(hq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return hq
}

// HelpGroupBy is the group-by builder for Help entities.
type HelpGroupBy struct {
	selector
	build *HelpQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hgb *HelpGroupBy) Aggregate(fns ...AggregateFunc) *HelpGroupBy {
	hgb.fns = append(hgb.fns, fns...)
	return hgb
}

// Scan applies the selector query and scans the result into the given value.
func (hgb *HelpGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hgb.build.ctx, "GroupBy")
	if err := hgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HelpQuery, *HelpGroupBy](ctx, hgb.build, hgb, hgb.build.inters, v)
}

func (hgb *HelpGroupBy) sqlScan(ctx context.Context, root *HelpQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hgb.fns))
	for _, fn := range hgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hgb.flds)+len(hgb.fns))
		for _, f := range *hgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HelpSelect is the builder for selecting fields of Help entities.
type HelpSelect struct {
	*HelpQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hs *HelpSelect) Aggregate(fns ...AggregateFunc) *HelpSelect {
	hs.fns = append(hs.fns, fns...)
	return hs
}

// Scan applies the selector query and scans the result into the given value.
func (hs *HelpSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hs.ctx, "Select")
	if err := hs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HelpQuery, *HelpSelect](ctx, hs.HelpQuery, hs, hs.inters, v)
}

func (hs *HelpSelect) sqlScan(ctx context.Context, root *HelpQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hs.fns))
	for _, fn := range hs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
